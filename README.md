# county
aijewl

## Setup

To connect this repository to a remote origin and push:

```bash
git remote add origin https://github.com/blazebbq/county.git
git branch -M main
git push -u origin main
```
